<resources>

    <declare-styleable name="SwipeCardsView">
        <!-- y_offset_step 定义的是卡片之间在y轴方向上的偏移量，单位是dp。
       举个例子，可见的卡片有3个，如果步长是20dp，从前往后看，卡片y轴坐标会依次增加20dp，表现上就是后面一张卡片底部有20dp会露出来
       如果值是负的，如 -20dp，那么表现则相反。
        如果不需要对卡片进行y轴方向上的偏移量处理，不设置这个属性或者设置为0dp就可以了-->
        <attr name="y_offset_step" format="dimension" />

        <!--<attr name="rotation_degrees" format="float" />-->

        <!-- scale_offset_step 定义的取值范围是0-1，所以scale的步长也得在这个范围之内，
        举个例子，可见的卡片有3个，如果步长是0.08，那么最前面的scale是1，后面一点的是0.92，最后面的是0.84
        值得注意的是 x 和 y同时被缩放了(1 - scaleStep*index)
        如果不需要对卡片进行缩放处理，不设置这个属性或者设置为0就可以了-->
        <attr name="scale_offset_step" format="float" />

        <!-- y_offset_step 定义的是 最大可见的卡片数。
        举个例子，假设定义为5，则正常状态重合状态下能看到的卡片数是4，当触摸移动过程中可以看到 底下4个 + 触摸中的1个 = 5个 -->
        <attr name="max_visible" format="integer" />

        <!-- min_adapter_stack 定义的是触发数据快空了的回调。
            举个例子，设置为6，则当adapter数据减少到6的时候，会触发 onAdapterAboutToEmpty 回调 -->
        <attr name="min_adapter_stack" format="integer" />

    </declare-styleable>
</resources>